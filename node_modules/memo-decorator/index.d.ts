export interface Resolver {
    (...args: any[]): any;
}
export interface MapLike<K = unknown, V = unknown> {
    set(key: K, v: V): MapLike<K, V>;
    get(key: K): V;
    has(key: K): boolean;
}
export interface Config {
    resolver?: Resolver;
    cache?: MapLike;
}
declare const _default: (config?: Config) => (_: any, __: string, descriptor: PropertyDescriptor) => PropertyDescriptor;
export default _default;
